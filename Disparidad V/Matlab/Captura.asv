function [ izq, der ] = Captura( cam_izq, cam_der )
%CAPTURA Summary of this function goes here
%   Detailed explanation goes here

    izq = getsnapshot(cam_izq);
    izq = rgb2gray(izq);
    der = getsnapshot(cam_der);
    der = rgb2gray(der);

    